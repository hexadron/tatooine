<%= render 'shared/alerts_to_js' %>

var sect = $('#section_<%= @section.id %>').closest('.section')

<% if @result %>
  sect.find('.done').each(function(i, e) {
    var el = $(e)
    var next = +el.text() + 1
    el.text(next)
  })
<% end %>