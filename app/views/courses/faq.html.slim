- @class_tab = 'faq'
- @show_primary_data = true

- content_for :more_for_courses do
  section#faq
    h2 Preguntas Frecuentes
    - if @course.faq.present?
      .faq-content== markdown @course.faq
    - else
      p No hay preguntas frecuentes todavía.